/**
 * @format
 */

import {AppRegistry} from 'react-native';
import AppJs from './AppJs';
// import {name as appName} from './app.json';

AppRegistry.registerComponent('parking_slip_v2', () => AppJs);
